# VADS 艺术藏品爬虫 · 使用指南

---

## 第一步：下载程序

1. 打开浏览器，进入本项目的 GitHub 页面
2. 点击顶部菜单栏的 **Actions**（操作）标签
3. 在左侧列表中点击 **Build Windows EXE**
4. 点击最新一次成功运行的记录（左侧显示绿色勾号 ✅）
5. 滚动到页面**最底部**，找到 **Artifacts**（产物）区域
6. 点击 **vads-crawler-windows** 下载压缩包
7. 解压后得到 `vads-crawler.exe`，双击即可运行，**无需安装任何软件**

---

## 第二步：运行程序

双击 `vads-crawler.exe`，程序会打开如下界面：

```
┌─────────────────────────────────────────┐
│         VADS Art Collection Crawler     │
├──────────────────────────────────────── │
│  Collections（藏品集）                   │
│  Settings（设置）                        │
│  [▶ Start Crawl]  [⏹ Stop]             │
│  Progress（进度条）                      │
│  Log（日志）                             │
└─────────────────────────────────────────┘
```

---

## 第三步：选择藏品集

**方式一：从网站加载列表（推荐）**

1. 点击 **Load collections from website** 按钮
2. 等待几秒，列表会自动填充所有可用藏品集
3. 在列表中点击你想要下载的藏品集
4. 如需选择多个，按住 **Ctrl** 键再点击其他条目

**方式二：手动输入**

在 *Or type aliases manually* 输入框中直接输入藏品集代码，多个代码用空格隔开，例如：

```
BDC IWM AAVAA
```

常用藏品集代码：

| 代码 | 藏品集名称 |
|------|-----------|
| `BDC` | Basic Design Collection |
| `IWM` | Imperial War Museum |
| `AAVAA` | African and Asian Visual Artists Archive |
| `CSC` | Crafts Study Centre |

---

## 第四步：调整设置（可选）

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| **Item limit** | 每个藏品集最多下载多少件，填 `0` 表示全部下载 | `0`（全部）|
| **Request delay** | 每次请求之间等待的秒数，数值越大对服务器越友好 | `0.3` 秒 |
| **Images folder** | 图片保存的文件夹路径，点击 **Browse…** 选择位置 | `images/` |
| **Database file** | 数据库文件路径（中间临时存储用，完成后自动清除） | `ica.db` |

---

## 第五步：开始下载

1. 点击 **▶ Start Crawl** 按钮
2. 进度条和日志会实时显示下载进度
3. 如需中途停止，点击 **⏹ Stop** 按钮，已下载的数据会保留

---

## 第六步：查看结果

下载完成后，程序会自动在 `vads-crawler.exe` 所在的文件夹生成：

- **`藏品集代码.csv`**（如 `BDC.csv`）  
  用 Excel 或 WPS 直接打开，包含每件藏品的所有文字信息（标题、艺术家、日期、描述等）

- **`images/藏品集代码/`** 文件夹（如 `images/BDC/`）  
  包含每件藏品的封面图片，文件名为藏品编号（如 `12.jpg`）  
  CSV 文件中的 `image_path` 列记录了每张图片对应的路径

---

## 常见问题

**Q：点击"Load collections"后没有反应？**  
A：请检查网络连接是否正常，稍后再试。也可以直接在输入框手动填写藏品集代码。

**Q：下载到一半程序关闭了，数据会丢失吗？**  
A：不会。重新打开程序，选择同一藏品集再次运行，程序会自动跳过已下载的内容，从断点继续。

**Q：CSV 文件用 Excel 打开后中文显示乱码？**  
A：打开 Excel → 数据 → 自文本/CSV → 选择文件 → 文件原始格式选择 **65001: Unicode (UTF-8)** → 完成导入。

**Q：图片文件夹在哪里？**  
A：默认保存在 `vads-crawler.exe` 同目录下的 `images/` 文件夹，可在设置中的 **Images folder** 修改位置。
